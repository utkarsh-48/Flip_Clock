{
  "name": "flip-clock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-icons": "node scripts/sync-icons.mjs",
    "dev": "node scripts/sync-icons.mjs && vite",
    "build": "node scripts/sync-icons.mjs && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
